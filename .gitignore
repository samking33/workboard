.idea/
.idea/*/*
.idea/httpRequests
config.yml
config.yaml
config.yml.sample
!docs/config.yml
!.github/ISSUE_TEMPLATE/config.yml
!.gitea/ISSUE_TEMPLATE/config.yml
docs/themes/
*.db
*.db-shm
*.db-wal
Run
# Scoped rather than a bare `dist/`: the built web client is committed so the
# deploy target never has to run a frontend build (and so never needs pnpm).
/dist/
desktop/dist/
cover.*
/vikunja
Test_*
bin/
secrets
*.deb
debian/
logs/
docs/public/
docs/resources/
pkg/static/templates_vfsdata.go
files/
!pkg/files/
!pkg/web/files/
vikunja-dump*
vendor/
os-packages/
mage_output_file.go
mage-static
.DS_Store
/plugins/*
/plugins-dev/*

# pnpm
.pnpm-store/

# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# AI Tools
/.claude/settings.local.json
PLAN.md
plans/
/.crush/
/.playwright-mcp

# Local dev build produced by `go build -o vikunja-dev`
/vikunja-dev

# Production build output and local secrets
/deploy/dist/
/deploy/*.env
!/deploy/.env.example

# Runtime data — never commit an instance's database, uploads or live config.
*.db
*.db-wal
*.db-shm
/files/
/config.yml
/config.yaml
/vikunja.env

# Never commit a real environment file — this repo is public.
.env
.env.*
!.env.example
